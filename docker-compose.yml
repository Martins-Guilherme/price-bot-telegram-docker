services:
  app:
    build:
      context: /home/martinss/vendaOnline
      dockerfile: /home/martinss/Docker/sqlite-bot/Dockerfile
    container_name: price-bot
    volumes:
      - ./db:/database
    ports:
      - "3000:3000"
    env_file:
      - .env
    working_dir: /app
    command: node src/index.js
